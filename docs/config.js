// Edit this after deploying your Cloudflare Worker.
// API_BASE is the Worker URL, e.g. "https://wishlist-api.yourname.workers.dev"
window.WISHLIST_CONFIG = {
  API_BASE: "https://wishlist-api.workers.dev",

  // Optional: the page title shown at the top.
  TITLE: "My Wishlist",
};
